spring:
    config:
      activate:
        on-profile:
        - dev
server:
  port: 8080
logging:
  level:
    hibertnate:
        SQL: debug
        type: trace
app:
    jwt:
        secret: abcdefghijklmnOPQRSTUVWXYZ